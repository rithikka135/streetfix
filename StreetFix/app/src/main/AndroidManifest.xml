<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Permissions -->
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION"/>
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION"/>
    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.StreetFix">

        <!-- LOGIN SCREEN -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <!-- USER LOGIN -->
        <activity android:name=".UserLoginActivity" android:exported="false"/>
        <!-- OFFICER LOGIN -->
        <activity android:name=".OfficerLoginActivity" android:exported="false"/>
        <!-- MAIN USER DASHBOARD -->
        <activity android:name=".MainActivity" android:exported="false"/>
        <!-- REPORT ISSUE -->
        <activity android:name=".ReportIssueActivity" android:exported="false"/>
        <!-- VIEW REPORTS -->
        <activity android:name=".ViewReportsActivity" android:exported="false"/>
        <!-- ISSUE DETAIL -->
        <activity android:name=".IssueDetailActivity" android:exported="false"/>
    </application>
</manifest>